import React from "react";
import { Container, Typography } from "@mui/material";

const Integration = () => {
  return (
    <Container>
      <Typography variant="h3" sx={{ mt: 5 }}>Integration</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Step-by-step guide on integrating the SDK with Swift and Objective-C applications.
      </Typography>
    </Container>
  );
};

export default Integration;
